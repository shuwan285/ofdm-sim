# OFDM 仿真 · 知识点清单与词汇表

> 本清单覆盖本项目涉及的全部知识点、定义和专业词汇，按主题分类，作为学习参考与速查手册。

## 一、信号与数学基础

| 术语 | 定义 |
|---|---|
| **傅里叶变换 (FT)** | 把连续信号变换到连续频率，告诉你"信号由哪些频率成分组成、各多强" |
| **离散傅里叶变换 (DFT)** | 计算机处理的信号是离散采样点，DFT 把 N 个时域采样点变成 N 个频域值（离散进、离散出） |
| **快速傅里叶变换 (FFT)** | 计算 DFT 的快速算法，结果与 DFT 完全相同，只是算得快 |
| **逆变换 (IFFT/IDFT)** | DFT/FFT 的逆运算，频域 → 时域。OFDM 发射端用它把 N 个子载波"打包"成时域波形 |
| **时域 / 频域** | 时域 = 信号随时间怎么变；频域 = 信号由哪些频率成分组成 |
| **采样定理（奈奎斯特）** | 采样率 fs 必须 ≥ 2 × 信号最高频率，否则发生混叠 |
| **混叠 (Aliasing)** | 采样率不足时，高频信号"伪装"成低频信号，无法区分 |
| **复数 / 复平面** | 复数 = 实部 + j·虚部，在复平面上是一个点，位置由幅度和相位决定 |
| **IQ 两路** | I（同相，cos 载波 = 实部）和 Q（正交，sin 载波 = 虚部），两路差 90°，可同时各传一路 |
| **欧拉公式** | e^{jθ} = cosθ + j·sinθ，把旋转和复数联系起来，是傅里叶变换的数学基础 |
| **正弦波三要素** | 幅度、频率、相位。调制就是改变这三者之一来承载数据 |

## 二、数字调制

| 术语 | 定义 |
|---|---|
| **调制 (Modulation)** | 把数据"写"到载波上：改幅度 = ASK、改相位 = PSK、改频率 = FSK |
| **载波 (Carrier)** | 用于承载数据的高频正弦波 |
| **码元/符号 (Symbol)** | 一个"信号单位"，代表一组比特（QPSK 一个码元 = 2 比特，16-QAM = 4 比特） |
| **码元速率 / 比特率** | 码元速率 = 每秒多少码元（发得快慢）；比特率 = 每秒多少比特 |
| **QPSK（四相移相键控 = 4-QAM）** | 用 4 个相位（复平面 4 个点）表示 2 个比特，点分布在四个象限 |
| **QAM（正交幅度调制）** | 同时调幅度和相位（等价于同时调 I、Q 两路），能在复平面上摆更多点 |
| **M-QAM** | 有 M 个星座点的 QAM，每符号携带 log₂(M) 个比特。M 越大速率越高，但点越密、越怕噪声 |
| **星座图 (Constellation)** | 把调制符号画在复平面上的图，每个点代表一个符号 |
| **格雷码 (Gray Code)** | 相邻星座点只差 1 个比特的编码，减少误判时的比特错误数 |

## 三、OFDM 核心

| 术语 | 定义 |
|---|---|
| **OFDM（正交频分复用）** | 把一路高速数据拆成 N 路低速数据，放到 N 个正交子载波上并行传输 |
| **子载波 (Subcarrier)** | OFDM 中每个独立的载波，各携带一路数据 |
| **子载波间隔 Δf** | 相邻子载波的频率差，由正交条件推出：Δf = 1/T（T 为码元持续时间） |
| **子载频 f_k** | 第 k 个子载波的频率：f_k = k/T = k·Δf |
| **正交 (Orthogonal)** | 两个信号相乘后在码元周期内积分 = 0，即互不干扰、可无干扰分离 |
| **频分复用 (FDM)** | 把频带切成多段、每段一路数据。传统 FDM 需保护频带，OFDM 靠正交省去 |
| **串并/并串转换 (S/P, P/S)** | 串并 = 一串 → N 路并行（准备 N 个子载波数据）；并串 = N 路 → 一串（发出） |
| **循环前缀 (CP)** | 把符号末尾复制到前面。长度 > 最大多径延迟时可消除码间串扰 |
| **码间串扰 (ISI)** | 前一个符号的"尾巴"（多径延迟）叠到后一个符号上，造成误判 |
| **子载波间干扰 (ICI)** | 无 CP 时，多径使子载波间不再正交，互相干扰 |
| **循环卷积 vs 线性卷积** | 加 CP 后，多径的线性卷积变循环卷积，FFT 后每子载波只被乘一个复数，简单均衡即可恢复 |

## 四、信道

| 术语 | 定义 |
|---|---|
| **AWGN（加性高斯白噪声）** | 加性 = 噪声"加"到信号上；高斯 = 取值服从高斯分布；白 = 各频率均匀 |
| **多径 (Multipath)** | 信号经多条路径（直射、反射）到达，时间不同，叠加造成干扰 |
| **衰落 (Fading)** | 多径叠加导致信号幅度波动。频率选择性衰落 = 不同频率衰减不同（需均衡） |
| **信噪比 SNR / Eb/N0** | SNR = 信号功率/噪声功率；Eb/N0 = 每比特能量/噪声功率谱密度（BER 曲线横轴） |
| **分贝 (dB)** | 对数单位。+10 dB = 10 倍，+3 dB ≈ 2 倍 |

## 五、接收与性能

| 术语 | 定义 |
|---|---|
| **误码率 (BER)** | 错误比特数 / 总比特数，衡量系统可靠性 |
| **信道估计 (Channel Estimation)** | 接收端用导频估计信道频率响应 H[k] |
| **导频 (Pilot)** | 发射端插入的"已知符号"，接收端用它估计信道 |
| **LS 估计（最小二乘）** | 在导频处用 H = Y/X 估计信道，最简单的估计方法 |
| **插值 (Interpolation)** | 把导频处估计的 H 连成整条 H[k] |
| **均衡 (Equalization)** | 用估计的信道 H[k] 消除信道影响（FFT 后除以 H[k]） |
| **PAPR（峰值平均功率比）** | 峰值功率 / 平均功率。OFDM 子载波同相相加产生高峰值，是 OFDM 著名缺点 |
| **CCDF（互补累积分布函数）** | P(PAPR > 阈值) 随阈值变化的曲线，用于刻画 PAPR 分布 |
| **误差地板 (Error Floor)** | 信噪比再高误码率也降不下去的现象（如 CP 不足时的 ISI） |
| **频带利用率 η** | 比特率 / 带宽，衡量频谱使用效率（OFDM 因正交而更高） |

## 六、核心公式汇总

1. **正交条件**：∫₀ᵀ cos(2πf_m t)·cos(2πf_n t) dt = 0（m ≠ n）
2. **子载波间隔**：Δf = 1/T
3. **子载波频率**：f_k = k/T = k·Δf
4. **误码率**：BER = 错误比特数 / 总比特数
5. **PAPR**：PAPR = max|x[n]|² / mean|x[n]|²
6. **频带利用率**：η = R_b / B

## 七、一句话总结 OFDM

> OFDM = 用 **IFFT** 把数据放到一堆**正交子载波**上并行发送，靠**循环前缀**抵抗多径干扰，接收端用 **FFT** 拆开。
