# OFDM 学习笔记（对照《通信原理》第7版 第8章 8.3节）

> 这份笔记把 OFDM 需要的上下文整理好了，配合教材看。不用背公式，重在懂"为什么"。

---

## 一、基础概念（课本默认你会，先补上）

### 1. 调制：怎么用"波"运数据

一个正弦波由 **频率 + 幅度 + 相位** 决定。调制 = 把数据"写"到波上：

- 改**幅度** → ASK（幅度键控）
- 改**相位** → PSK（相位键控）
- 改**频率** → FSK（频率键控）

**QPSK** = 用 4 个不同相位代表 00/01/11/10（就是星座图上那 4 个点）。

> 🚆 类比：载波是火车，数据是乘客，调制 = 让乘客上车；幅度/相位/频率就是"座位号"。

### 2. 频分复用（FDM）：一根线传多路

把整个频带切成多段，每段用不同频率的载波传一路数据，互不重叠。

> 🛣️ 类比：一条高速公路（频带）画成多条车道（子频带），每辆车走自己的车道。

**传统 FDM 的缺点**：车道之间必须留很宽的"保护频带"防止撞车（干扰），浪费频带。

### 3. 正交：整数个周期 → 互不干扰

在一个观察时间 **T** 内，两个正弦波都**正好走完整数个周期**、周期数不同，则它们**正交**（互不干扰）。

OFDM 的关键选择：让所有子载波频率**等间隔**排列（f₁=1/T，f₂=2/T，f₃=3/T，……），天然两两正交。

> 🧠 类比：两个齿轮，一个每秒转 1 圈、另一个每秒转 2 圈。观察 1 秒都回到原点，平均相互作用抵消为 0。

---

## 二、OFDM 原理

### 1. 基本思想：一路快数据 → N 路慢数据，并行发

```
传统单载波：   [一路大数据] ──► 一个宽带载波，一次发完（怕多径）

OFDM：        [一路大数据] ──► 拆成 N 路小数据
                  ├─► 子载波 f₁
                  ├─► 子载波 f₂    → 全部叠加，同时发射
                  └─► 子载波 f_N
               接收端用 FFT 拆开，各取各的数据
```

### 2. 为什么正交能"省频带"（最大优势）

子载波正交 → 频谱可以**重叠却互不干扰**。每个子载波的峰值处，其他子载波恰好为 0，所以能完美分开。结果：同样频带塞下更多数据，频谱利用率更高。

```
传统 FDM（浪费）：  ██   ██   ██   ██     ← 有保护频带
OFDM（重叠）：      ╱╲╱╲╱╲╱╲             ← 波形重叠，但互不干扰
```

### 3. 用 IFFT/FFT 实现（工程天才想法）

把 N 个数据符号当成"频域"的 N 个点，做一次 **IFFT** 就得到时域叠加好的信号；接收端做 **FFT** 还原。

- 发射端 **IFFT** = 频域(数据) → 时域(波形)，即"打包"
- 接收端 **FFT** = 时域(波形) → 频域(数据)，即"拆包"

---

## 三、多径与循环前缀

### 1. 什么是多径

信号从发射机到接收机，可能走直线（视距）、撞楼/山反射、甚至多次反射。不同路径长度不同 → 到达时间不同 → 接收端收到**好几个"延迟版"叠加**的信号。

> 🗣️ 类比：空房间喊一声，先听到直射声，再听到墙壁回声，叠起来就"糊"了。

### 2. 多径的问题：码间串扰（ISI）

前一个符号走了慢的反射路径，它的"尾巴"来晚了，叠到后一个符号上，导致后一个符号被污染、解调判错。

```
发送端：    [ 符号1 ][ 符号2 ]
直射路径：  [ 符号1 ][ 符号2 ]          ← 准时到
反射路径：         [ 符号1 ][ 符号2 ]    ← 晚了半拍
接收端：    [ 符号1 ][ 符号2 + 符号1的尾巴 ]  ← 被污染！
```

### 3. 循环前缀（CP）的解决

在每个符号**前面**垫一段（复制它自己的**末尾**）：

```
原符号：        [ A B C D ]
加循环前缀后：   [ C D | A B C D ]
                  ↑CP    ↑有效数据（接收端只留这部分，CP 丢掉）
```

只要 **CP 长度 > 最大多径延迟**：

1. **消灭 ISI**：前一个符号的"尾巴"只落在 CP 区，CP 被丢掉，有效数据不受污染。
2. **数学魔法**：多径从"线性卷积"变"循环卷积"，FFT 后每个子载波只是被乘了一个复数 → 除回这个复数即可恢复（这就是 M5 的"均衡"）。

---

## 四、一句话总结

> OFDM = 用 **IFFT** 把数据放到一堆**正交子载波**上并行发送，靠**循环前缀**抵抗多径干扰，接收端用 **FFT** 拆开。

---

## 五、对应里程碑（方便你回来写代码时定位）

| 里程碑 | 内容 | 用到的知识点 |
|---|---|---|
| M1 信号基础 | FFT / 采样 / 复数 | 笔记一、3 |
| M2 数字调制 | QPSK/QAM、星座图 | 笔记一、1 |
| M3 OFDM 核心 | IFFT/FFT + 循环前缀 | 笔记二、三 |
| M4 加信道 | AWGN + 多径 | 笔记三、2 |
| M5 进阶 | 信道估计 + 均衡 + PAPR | 笔记三、3 |
